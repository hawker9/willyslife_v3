code for psycog posting:

import os
import psycopg2

DATABASE_URL = os.environ['DATABASE_URL']
conn = psycopg2.connect(DATABASE_URL, sslmode='require')

#Creating a cursor object using the cursor() method
cursor = conn.cursor()

#Creating table as per requirement
sql2 = '''INSERT INTO votes (name) VALUES ('TEST');'''


cursor.execute(sql2)

print("Table created successfully........")

#Closing the connection
conn.close()



To watch the heroku server type in "heroku logs --tail -a willyslife" 


databaseadd sexction of the python code is the one that integrates with the post grs SQL